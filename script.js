window.addEventListener('load', () => {
      setTimeout(() => {
        document.getElementById('image').classList.add('reveal');
      }, 500); // Trigger image reveal after plane starts
    });    